# BOOKSHELF 개발 현황

최종 갱신: 2026-09-18

## 현재 구현됨

### 책 관리

- 책 제목, 작가, 도서 분류, 읽기 상태를 등록할 수 있음
- 도서 분류: 소설, 역사, 인문, 사회, 과학, 예술, 외국어, 자기계발, 기타
- 읽고 싶은 책에는 담은 날짜를, 읽는 중·읽은 책에는 읽기 시작일과 완료일을 저장할 수 있음
- 읽기 시작일과 읽은 날짜를 저장해 완독 기간을 계산할 수 있음
- 읽기 상태를 나중에 `읽은 책`으로 바꾸면 오늘 날짜가 자동 저장됨
- 책 카드에서 제목, 작가, 분류, 읽은 날짜를 수정할 수 있음
- 제목·작가가 같은 중복 책 등록을 방지함

### 표지 자동 검색

- 새 책 등록 시 서버를 통해 Google Books API로 표지를 자동 검색함
- 표지 URL과 ISBN을 브라우저 저장소에 보관함
- 검색 결과가 없거나 이미지를 불러올 수 없으면 기본 책 아이콘을 표시함
- 기존 책은 `수정` 후 저장하면 표지를 다시 검색함

### 책장 요약

- 작가별·분류별 권수를 표시함
- 읽는 중 권수, 완독 권수, 평균 완독 기간을 표시함
- 작가별 항목을 누르면 해당 작가의 `읽은 책`과 읽은 날짜를 표시함
- 책장 요약은 화면의 마지막 챕터임

### 독서 기록

- 책 제목, 별점, 기록한 날짜, 한줄평, 인용문, 생각을 저장할 수 있음
- 기록한 날짜는 기본값으로 오늘 날짜가 입력되며 변경 가능함

### 독서 달력

- 읽은 날짜를 기준으로 월별 독서 달력을 표시함
- 날짜 칸에 해당 날짜에 읽은 책 표지를 최대 3권까지 표시함
- 날짜를 누르면 그날 읽은 책 목록을 표시함

### AI 추천

- Gemini API를 이용한 책 한 권 추천 기능이 있음
- Vercel Python 서버리스 함수(`/api/recommend`)를 사용함

## 데이터와 배포 상태

- 책 목록과 독서 기록은 현재 브라우저 `localStorage`에 저장됨
- 같은 브라우저에서는 새로고침 후에도 유지됨
- 다른 기기·다른 브라우저·다른 사람과는 데이터가 공유되지 않음
- Vercel 배포 주소는 현재 로컬 변경사항이 반영되기 전 버전임
- API 키는 코드나 이 문서에 기록하지 않음

## 다음 작업 우선순위

1. 카카오 책 검색 API를 보조 검색으로 추가해 한국 도서 표지 정확도 개선
2. 읽은 책의 작가·분류·별점을 활용한 취향 분석
3. 취향 분석 결과를 AI 추천 요청에 반영

## 향후 공유 기능

- 로컬 기능을 충분히 완성한 뒤 로그인·온라인 데이터베이스를 추가함
- 목표: 개인 기록은 비공개로 유지하고, 선택한 책장만 다른 사람에게 공유
- 후보 서비스: Supabase 등 인증·데이터베이스 서비스

## 카카오 책 검색 연동 전 준비물

- Kakao Developers 개발자 계정과 애플리케이션 등록
- REST API 키
- 키는 Vercel 환경 변수 `KAKAO_REST_API_KEY`로 저장
- 브라우저 JavaScript에 키를 직접 넣지 않음

## 검증 상태

- `node --check js/app.js` 문법 검사 통과
- Git diff 공백 오류 검사 통과
- 브라우저 실사용 및 Vercel 재배포 확인은 아직 진행 전

## Google Books API 설정

- 로컬 `.env`에 `GOOGLE_BOOKS_API_KEY`를 설정함
- 키는 `/api/book-search` 서버 함수에서만 사용하며 브라우저에 노출하지 않음
- Vercel 배포 시에도 같은 이름의 환경 변수를 추가해야 함
- `.vercelignore`로 `.env`와 로컬 환경 파일의 업로드·서빙을 차단함
